# Let's get anythinig not necessary out of the container for faster dockerisation

# Git related files are obviously not necessary inside container and there are a LOT of them !
.git*


# 'Junk' generated by Rails project
db/*.sqlite3
db/*.sqlite3-journal
log/*
tmp/*

# Docker related files (not needed inside containers)
Dockerfile
docker/*

# Project related but not necessary for running it
README.md
